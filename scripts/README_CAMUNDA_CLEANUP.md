# Очистка данных Camunda

Этот скрипт позволяет очистить различные типы данных из базы данных Camunda.

## ⚠️ ВНИМАНИЕ

**Операция необратима!** Перед выполнением убедитесь, что:
- У вас есть резервная копия базы данных
- Вы понимаете последствия удаления исторических данных
- Camunda сервер остановлен или вы готовы к возможным проблемам

## 📋 Требования

- Python 3.8+
- Библиотека `psycopg2` или `psycopg2-binary`
- Доступ к базе данных PostgreSQL с правами на удаление данных

## 🚀 Использование

### Настройка перед первым запуском

1. **Создайте файл конфигурации** в директории `scripts/`:

```bash
cd scripts
cp .env.example .env
```

2. **Отредактируйте `scripts/.env`** и укажите реальные значения:

```env
POSTGRES_HOST=localhost          # или postgresql для Docker сети
POSTGRES_PORT=5432
CAMUNDA_DATABASE_NAME=camunda
CAMUNDA_DATABASE_USER=camunda
CAMUNDA_DATABASE_PASSWORD=your_real_password
```

**Важно:** 
- Если база данных в Docker, используйте `POSTGRES_HOST=postgresql`
- Если база данных локальная, используйте `POSTGRES_HOST=localhost`

### Вариант 1: Прямой запуск (локально)

```bash
# Установка зависимостей (если еще не установлены)
uv add psycopg2-binary

# Проверка (без удаления данных) - только история
python scripts/cleanup_camunda_history.py --dry-run

# Очистка истории и активных процессов
python scripts/cleanup_camunda_history.py --history --runtime --verbose

# Полная очистка всех данных (кроме identity)
python scripts/cleanup_camunda_history.py --all

# Очистка только определений процессов
python scripts/cleanup_camunda_history.py --repository
```

### Вариант 2: Через Docker Compose

Если база данных запущена через Docker Compose:

```bash
cd Docker-compose

# Проверка (dry-run)
docker compose exec app python scripts/cleanup_camunda_history.py --dry-run

# Очистка
docker compose exec app python scripts/cleanup_camunda_history.py
```

### Вариант 3: Через Docker (отдельный контейнер)

```bash
# Проверка
docker run --rm \
  --network mysed_services_network \
  -e POSTGRES_HOST=postgresql \
  -e POSTGRES_PORT=5432 \
  -e CAMUNDA_DATABASE_NAME=camunda \
  -e CAMUNDA_DATABASE_USER=camunda \
  -e CAMUNDA_DATABASE_PASSWORD=your_password \
  -v $(pwd):/app \
  -w /app \
  python:3.11-slim \
  sh -c "pip install psycopg2-binary && python scripts/cleanup_camunda_history.py --dry-run"

# Очистка
docker run --rm -it \
  --network mysed_services_network \
  -e POSTGRES_HOST=postgresql \
  -e POSTGRES_PORT=5432 \
  -e CAMUNDA_DATABASE_NAME=camunda \
  -e CAMUNDA_DATABASE_USER=camunda \
  -e CAMUNDA_DATABASE_PASSWORD=your_password \
  -v $(pwd):/app \
  -w /app \
  python:3.11-slim \
  sh -c "pip install psycopg2-binary && python scripts/cleanup_camunda_history.py"
```

## ⚙️ Переменные окружения

Скрипт автоматически загружает переменные окружения из `.env` файла. Приоритет поиска:

1. **`scripts/.env`** (приоритет) - файл конфигурации рядом со скриптом
2. **`Docker-compose/.env`** - основной файл конфигурации Docker Compose
3. **`.env`** в корне проекта
4. Переменные окружения системы

### Создание файла конфигурации

Создайте файл `scripts/.env` на основе примера:

```bash
cd scripts
cp .env.example .env
# Отредактируйте .env и укажите реальные значения
```

Пример содержимого `scripts/.env`:

```env
# Настройки подключения к PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
CAMUNDA_DATABASE_NAME=camunda
CAMUNDA_DATABASE_USER=camunda
CAMUNDA_DATABASE_PASSWORD=your_password_here
```

**Важно:** 
- Если база данных в Docker, используйте `POSTGRES_HOST=postgresql` (имя сервиса)
- Если база данных локальная, используйте `POSTGRES_HOST=localhost`
- Файл `scripts/.env` имеет приоритет над другими .env файлами

### Поддерживаемые переменные

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `POSTGRES_HOST` | Хост PostgreSQL | `localhost` |
| `POSTGRES_PORT` | Порт PostgreSQL | `5432` |
| `CAMUNDA_DATABASE_NAME` | Имя базы данных Camunda | `camunda` |
| `CAMUNDA_DATABASE_USER` | Пользователь БД | `camunda` |
| `CAMUNDA_DATABASE_PASSWORD` | Пароль БД | **(обязательно)** |

### Автоматическая загрузка из .env

Скрипт автоматически найдет и загрузит переменные из `.env` файла. Приоритет поиска:

1. `scripts/.env` (рекомендуется) - файл рядом со скриптом
2. `Docker-compose/.env` - основной файл конфигурации
3. `.env` в корне проекта

```bash
# Создайте файл конфигурации
cd scripts
cp .env.example .env
# Отредактируйте .env и укажите реальные значения

# Запустите скрипт - он сам загрузит настройки
python scripts/cleanup_camunda_history.py --dry-run
```

### Ручная установка переменных окружения

Если нужно переопределить значения из .env файла:

```bash
export POSTGRES_HOST=localhost
export POSTGRES_PORT=5432
export CAMUNDA_DATABASE_NAME=camunda
export CAMUNDA_DATABASE_USER=camunda
export CAMUNDA_DATABASE_PASSWORD=your_password

python scripts/cleanup_camunda_history.py --dry-run
```

**Примечание:** Переменные окружения имеют приоритет над значениями из .env файла.

## 📊 Типы данных и что удаляется

Скрипт поддерживает очистку различных типов данных Camunda:

### 1. Исторические данные (ACT_HI_*) - по умолчанию

Удаляет данные о завершенных процессах:
- `ACT_HI_PROCINST` - экземпляры процессов
- `ACT_HI_ACTINST` - экземпляры действий
- `ACT_HI_TASKINST` - экземпляры задач
- `ACT_HI_VARINST` - переменные процесса
- `ACT_HI_DETAIL` - детали истории
- `ACT_HI_COMMENT` - комментарии
- `ACT_HI_ATTACHMENT` - вложения
- `ACT_HI_IDENTITYLINK` - связи идентификации
- `ACT_HI_INCIDENT` - инциденты
- `ACT_HI_JOB_LOG` - логи заданий
- `ACT_HI_BATCH` - пакетные операции

**Использование:** `--history` или по умолчанию

### 2. Runtime данные (ACT_RU_*)

Удаляет данные об **активных** процессах и задачах:
- `ACT_RU_EXECUTION` - активные выполнения процессов
- `ACT_RU_TASK` - активные задачи
- `ACT_RU_VARIABLE` - переменные активных процессов
- `ACT_RU_JOB` - активные задания
- `ACT_RU_INCIDENT` - активные инциденты
- `ACT_RU_IDENTITYLINK` - связи идентификации для активных процессов

⚠️ **ВНИМАНИЕ:** Удаление этих данных приведет к потере всех активных процессов и задач!

**Использование:** `--runtime`

### 3. Repository данные (ACT_RE_*)

Удаляет определения процессов (деплойменты):
- `ACT_RE_DEPLOYMENT` - деплойменты
- `ACT_RE_PROCDEF` - определения процессов
- `ACT_RE_CASE_DEF` - определения кейсов
- `ACT_RE_DECISION_DEF` - определения решений

⚠️ **ВНИМАНИЕ:** После удаления нужно будет заново развернуть все процессы!

**Использование:** `--repository`

### 4. Identity данные (ACT_ID_*)

Удаляет информацию о пользователях и группах:
- `ACT_ID_USER` - пользователи
- `ACT_ID_GROUP` - группы
- `ACT_ID_MEMBERSHIP` - членства в группах
- `ACT_ID_INFO` - дополнительная информация

⚠️ **ВНИМАНИЕ:** Обычно эти данные НЕ удаляют, так как они могут быть связаны с LDAP!

**Использование:** `--identity`

### 5. General данные (ACT_GE_*)

Удаляет общие данные и байт-массивы:
- `ACT_GE_BYTEARRAY` - байт-массивы (BPMN файлы, диаграммы)
- `ACT_GE_PROPERTY` - свойства движка

**Использование:** `--general`

### Комбинированное использование

```bash
# Очистка истории и активных процессов
--history --runtime

# Полная очистка всех данных (кроме identity)
--all

# Очистка только определений процессов
--repository
```

## 🔍 Режим проверки (dry-run)

Перед реальной очисткой рекомендуется сначала запустить скрипт в режиме проверки:

```bash
python scripts/cleanup_camunda_history.py --dry-run --verbose
```

Это покажет:
- Какие таблицы будут очищены
- Сколько строк в каждой таблице
- Общее количество данных для удаления

**Данные при этом НЕ удаляются.**

## 📝 Пример вывода

```
============================================================
Очистка исторических данных Camunda
============================================================
Время запуска: 2025-01-15 10:30:00

Подключение к базе данных camunda на localhost:5432...

Найдено таблиц истории: 11
============================================================
  ACT_HI_ACTINST: 15,234 строк
  ACT_HI_PROCINST: 1,234 строк
  ACT_HI_TASKINST: 3,456 строк
  ...

============================================================
⚠️  ВНИМАНИЕ: Вы собираетесь удалить ВСЕ исторические данные!
============================================================
Таблиц для очистки: 11
Всего строк для удаления: 25,432

Операция НЕОБРАТИМА!

Продолжить? (yes/no): yes

Начинаем очистку...
============================================================
✓ ACT_HI_ACTINST
✓ ACT_HI_PROCINST
...

============================================================
✅ ОЧИСТКА ЗАВЕРШЕНА
============================================================
Таблиц обработано: 11
Строк удалено: 25,432
Строк осталось: 0
```

## 🔧 Параметры командной строки

```bash
python scripts/cleanup_camunda_history.py [OPTIONS]

Опции выбора типов данных:
  --history, -hi     Исторические данные (ACT_HI_*) - по умолчанию
  --runtime, -ru     Runtime данные (ACT_RU_*) - активные процессы
  --repository, -re  Repository данные (ACT_RE_*) - определения процессов
  --identity, -id    Identity данные (ACT_ID_*) - пользователи и группы
  --general, -ge     General данные (ACT_GE_*) - общие данные
  --all              Все данные кроме identity

Общие опции:
  --dry-run          Режим проверки (не удаляет данные)
  --verbose, -v      Подробный вывод информации
  --help, -h         Показать справку
```

### Примеры использования

```bash
# Только исторические данные (по умолчанию)
python scripts/cleanup_camunda_history.py

# История + активные процессы
python scripts/cleanup_camunda_history.py --history --runtime

# Полная очистка (кроме identity)
python scripts/cleanup_camunda_history.py --all

# Только определения процессов
python scripts/cleanup_camunda_history.py --repository

# Проверка перед полной очисткой
python scripts/cleanup_camunda_history.py --all --dry-run --verbose
```

## 💡 Рекомендации по очистке

### Сценарий 1: Очистка только истории (рекомендуется для продакшена)

Используйте когда нужно освободить место, но сохранить активные процессы:

```bash
python scripts/cleanup_camunda_history.py --history
```

**Что удаляется:** Только завершенные процессы и их история  
**Что сохраняется:** Активные процессы, задачи, определения процессов

### Сценарий 2: Полная очистка для тестовой среды

Используйте когда нужно полностью очистить базу для тестирования:

```bash
python scripts/cleanup_camunda_history.py --all
```

**Что удаляется:** История + активные процессы + определения процессов + общие данные  
**Что сохраняется:** Пользователи и группы (identity)

⚠️ **После этого нужно будет заново развернуть все процессы!**

### Сценарий 3: Очистка активных процессов

Используйте когда нужно остановить все активные процессы:

```bash
python scripts/cleanup_camunda_history.py --runtime
```

**Что удаляется:** Все активные процессы и задачи  
**Что сохраняется:** История, определения процессов

### Сценарий 4: Очистка определений процессов

Используйте когда нужно удалить старые версии процессов:

```bash
python scripts/cleanup_camunda_history.py --repository
```

⚠️ **После этого нужно будет заново развернуть все процессы!**

## 🛡️ Безопасность

1. **Резервное копирование**: Всегда создавайте резервную копию перед очисткой:
   ```bash
   cd Docker-compose
   docker compose exec postgresql pg_dump -U camunda camunda > backup_camunda_$(date +%Y%m%d_%H%M%S).sql
   ```

2. **Остановка Camunda**: Рекомендуется остановить Camunda перед очисткой:
   ```bash
   docker compose stop camunda
   ```

3. **Проверка**: Используйте `--dry-run` перед реальной очисткой:
   ```bash
   python scripts/cleanup_camunda_history.py --all --dry-run --verbose
   ```

4. **Очистка Identity**: Обычно НЕ рекомендуется очищать identity данные, так как они могут быть связаны с LDAP и другими системами.

## 🐛 Устранение проблем

### Ошибка подключения к БД

```
❌ Ошибка подключения к базе данных: ...
```

**Решение:**
- Проверьте, что PostgreSQL запущен
- Убедитесь, что параметры подключения корректны
- Проверьте сетевые настройки (если используете Docker)

### Ошибка прав доступа

```
❌ Ошибка при очистке ACT_HI_*: permission denied
```

**Решение:**
- Убедитесь, что пользователь БД имеет права на TRUNCATE
- Проверьте, что используется правильный пользователь (`camunda`)

### Таблицы не найдены

```
⚠️  Таблицы истории не найдены в базе данных.
```

**Решение:**
- Убедитесь, что подключены к правильной базе данных
- Проверьте, что Camunda была развернута и использовалась

## 📚 Дополнительная информация

- [Camunda History Cleanup](https://docs.camunda.org/manual/latest/user-guide/process-engine/history/)
- [PostgreSQL TRUNCATE](https://www.postgresql.org/docs/current/sql-truncate.html)

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи скрипта
2. Убедитесь, что все зависимости установлены
3. Проверьте доступность базы данных
4. Создайте issue в репозитории проекта

