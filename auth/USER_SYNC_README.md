# Синхронизация пользователей между Mayan EDMS и OpenLDAP

## Обзор

Система синхронизации пользователей позволяет автоматически синхронизировать пользователей и группы между Mayan EDMS и OpenLDAP. Это обеспечивает единообразное управление доступом к документам и процессам.

## Компоненты системы

### 1. MayanClient (services/mayan_connector.py)

Расширенный клиент для работы с Mayan EDMS API, включающий:

- **Аутентификация с API токенами**: Создание и отзыв пользовательских API токенов
- **Управление пользователями**: Создание, получение списка пользователей
- **Управление группами**: Создание, получение списка групп
- **Управление членством**: Добавление/удаление пользователей в/из групп

#### Ключевые методы:

```python
# Создание API токена для пользователя
mayan_client.create_user_api_token(username, password)

# Отзыв API токена
mayan_client.revoke_user_api_token(token)

# Управление группами
mayan_client.get_groups()
mayan_client.get_group_users(group_id)
mayan_client.add_user_to_group(group_id, username)
mayan_client.remove_user_from_group(group_id, username)

# Управление пользователями
mayan_client.get_users()
mayan_client.create_user(user_data)
```

### 2. UserSyncManager (services/user_sync_manager.py)

Менеджер синхронизации, который координирует процесс синхронизации между системами:

- **Синхронизация пользователей**: Создание и обновление пользователей
- **Синхронизация групп**: Создание и обновление групп
- **Синхронизация членства**: Управление членством пользователей в группах

#### Ключевые методы:

```python
# Синхронизация пользователей из LDAP в Mayan
sync_manager.sync_users_from_ldap_to_mayan()

# Синхронизация групп из LDAP в Mayan
sync_manager.sync_groups_from_ldap_to_mayan()

# Синхронизация членства в группах
sync_manager.sync_user_group_memberships()
```

### 3. DocumentAccessManager (services/document_access_manager.py)

Менеджер доступа к документам, который обеспечивает:

- **Проверка доступа**: Определение прав пользователя на документы
- **Фильтрация документов**: Показ только доступных пользователю документов
- **Управление разрешениями**: Настройка прав доступа

## API Endpoints Mayan EDMS

### Аутентификация

```
POST /auth/token/obtain/
Content-Type: application/json

{
    "username": "string",
    "password": "string"
}

Response:
{
    "username": "string",
    "password": "string",
    "token": "string"
}
```

### Управление группами

```
GET /api/v4/groups/                    # Получить список групп
GET /api/v4/groups/{group_id}/users/   # Получить пользователей группы
POST /api/v4/groups/{group_id}/users/add/    # Добавить пользователя в группу
POST /api/v4/groups/{group_id}/users/remove/ # Удалить пользователя из группы
```

### Управление пользователями

```
GET /api/v4/users/                     # Получить список пользователей
POST /api/v4/users/                    # Создать пользователя
```

## Использование

### 1. Тестирование подключения

```bash
python test_user_sync.py
```

Этот скрипт тестирует:
- Подключение к Mayan EDMS
- Получение списка групп и пользователей
- Операции добавления пользователей в группы

### 2. Синхронизация пользователей

```python
from services.mayan_connector import MayanClient
from services.user_sync_manager import UserSyncManager
from config.settings import config

# Создаем клиент Mayan EDMS
mayan_client = MayanClient(
    base_url=config.mayan_url,
    username=config.mayan_username,
    password=config.mayan_password,
    api_token=config.mayan_api_token,
    verify_ssl=False
)

# Создаем менеджер синхронизации
sync_manager = UserSyncManager(mayan_client, ldap_client)

# Синхронизируем пользователей
results = sync_manager.sync_users_from_ldap_to_mayan()
print(f"Создано пользователей: {len(results['created_users'])}")
print(f"Обновлено пользователей: {len(results['updated_users'])}")
print(f"Ошибок: {len(results['errors'])}")
```

### 3. Синхронизация групп

```python
# Синхронизируем группы
results = sync_manager.sync_groups_from_ldap_to_mayan()
print(f"Создано групп: {len(results['created_groups'])}")
print(f"Обновлено групп: {len(results['updated_groups'])}")
```

### 4. Синхронизация членства

```python
# Синхронизируем членство в группах
results = sync_manager.sync_user_group_memberships()
print(f"Добавлено членств: {len(results['added_memberships'])}")
print(f"Удалено членств: {len(results['removed_memberships'])}")
```

## Конфигурация

### Настройки Mayan EDMS (config/settings.py)

```python
# Mayan EDMS настройки
mayan_url = "https://mayan.example.com"
mayan_username = "mayan_login"
mayan_password = "mayan_password"
mayan_api_token = "your_api_token"
```

### Логирование

Все операции логируются в файл `logs/user_sync.log` с подробной информацией о:
- Подключениях к API
- Создании и обновлении пользователей/групп
- Ошибках и исключениях
- Результатах синхронизации

## Безопасность

### API Токены

- **Создание**: API токены создаются при аутентификации пользователя
- **Хранение**: Токены хранятся в сессии пользователя
- **Отзыв**: Токены автоматически отзываются при выходе пользователя
- **Время жизни**: Токены имеют ограниченное время жизни

### Аутентификация

- **LDAP**: Первичная аутентификация через OpenLDAP
- **Mayan EDMS**: Вторичная аутентификация через API токены
- **Сессии**: Управление сессиями через SessionManager

## Мониторинг

### Логи

Все операции записываются в лог с уровнем INFO и выше:

```
2025-01-10 21:30:15 - services.mayan_connector - INFO - 🔑 MayanClient: Создаем API токен для пользователя nabelova
2025-01-10 21:30:16 - services.mayan_connector - INFO - 🔑 MayanClient: API токен успешно создан для пользователя nabelova
2025-01-10 21:30:17 - services.user_sync_manager - INFO - 🔄 Начинаем синхронизацию пользователей из OpenLDAP в Mayan EDMS
```

### Метрики

Результаты синхронизации включают:
- Количество обработанных записей
- Количество созданных/обновленных записей
- Количество ошибок
- Время выполнения операций

## Устранение неполадок

### Частые проблемы

1. **Ошибка 401 (Unauthorized)**
   - Проверьте учетные данные Mayan EDMS
   - Убедитесь, что API токен действителен

2. **Ошибка 404 (Not Found)**
   - Проверьте правильность URL endpoints
   - Убедитесь, что группа/пользователь существует

3. **Ошибка 403 (Forbidden)**
   - Проверьте права доступа пользователя
   - Убедитесь, что пользователь имеет права на создание токенов

### Отладка

Включите подробное логирование:

```python
import logging
logging.getLogger('services.mayan_connector').setLevel(logging.DEBUG)
logging.getLogger('services.user_sync_manager').setLevel(logging.DEBUG)
```

## Разработка

### Добавление новых методов

1. Добавьте метод в `MayanClient`
2. Добавьте соответствующий метод в `UserSyncManager`
3. Обновите документацию
4. Добавьте тесты

### Тестирование

```bash
# Тест подключения
python test_user_sync.py

# Тест синхронизации
python -c "from services.user_sync_manager import UserSyncManager; ..."
```

## Лицензия

Проект использует лицензию проекта.
